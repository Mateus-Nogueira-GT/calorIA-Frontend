# Backend local (o PORT default do backend é 3000 — ver backend/.env.example).
# Em produção (build web na Vercel) o valor é sobrescrito para /api no build.
API_BASE_URL=http://localhost:3000
API_TIMEOUT=10000
