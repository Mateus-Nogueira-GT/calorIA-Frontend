{
  "buildCommand": "cd backend && npm ci && npm run build && cd ../app && npm ci && node -e \"require('fs').writeFileSync('.env', 'API_BASE_URL=/api\\nAPI_TIMEOUT=10000\\n')\" && npm run build:web",
  "outputDirectory": "app/web/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/handler" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/handler.js": {
      "maxDuration": 30,
      "includeFiles": "backend/node_modules/openai/**"
    }
  }
}
